# 合約橫盤縮量爆發偵測器 — 前端

部署到 Vercel 即可使用，會自動呼叫後端 API (Render) 進行掃描。

## 本機測試
```bash
npm install
npm run dev
```

## 部署到 Vercel
1. 把整個資料夾推上 GitHub（新建一個 repo，例如 `crypto-frontend`）
2. 到 vercel.com 用 GitHub 登入
3. New Project → Import 這個 repo
4. Framework Preset 選 **Vite**
5. 直接 Deploy，完成後拿到網址即可使用
